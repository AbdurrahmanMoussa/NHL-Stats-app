import Head from "next/head";
import { Fragment } from "react";

function HomePage({ data }) {
  /* TODO: Create Search */
  return (
    <Fragment>
      <Head>
        <title>NHL Player Search</title>
        <meta name="description" content="Search NHL Players" />
      </Head>
    </Fragment>
  );
}

export default HomePage;
